using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using UnityEngine;

[CreateAssetMenu(fileName = "Audio_config", menuName = "Settings/Audio_config")]
public class Audio_config : ScriptableObject
{
    public bool Audio_onOff = true; // Estado del audio, por defecto activado
}